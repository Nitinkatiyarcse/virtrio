import { ConvertToNewLinesPipe } from './convert-to-new-lines.pipe';

describe('ConvertToNewLinesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToNewLinesPipe();
    expect(pipe).toBeTruthy();
  });
});
